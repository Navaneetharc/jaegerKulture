<%- include('../partials/userL/header.ejs') %>

<style>
   
   .forgot-password-link {
    color: #007bff !important; 
    text-decoration: none; 
}

.forgot-password-link:hover {
    text-decoration: underline; 
    text-decoration-color: #007bff; 
}


</style>
<section class="content-main mt-80 mb-80">
    <div class="container">
        <!-- Logo placed at the top center of the page, slightly shifted right -->
        <div class="row justify-content-center mb-4">
            <div class="col-12 d-flex justify-content-center">
                <div class="logo logo-width-1" style="margin-right: 20px; text-align: center;">
                    <img src="assets/imgs/theme/jaeger.png" alt="logo" class="img-fluid" style="max-height: 100px;">
                </div>
            </div>
        </div>

        <!-- Login card in separate row -->
        <div class="row justify-content-center">
            <div class="col-md-6 col-lg-5">
                <!-- Login card -->
                <div class="card card-login" style="width: 100%;">
                    <div class="card-body">
                        <h4 class="card-title mb-4 text-center">Log in</h4>
                        
                        <form action="/login" method="post">
                            <div class="mb-3">
                                <input class="form-control" placeholder="Username or email" type="text" id="email" name="email">
                            </div>
                            <div class="mb-3">
                                <input class="form-control" placeholder="Password" type="password" id="password" name="password">
                                <div id="error2" class="error-message"></div>
                            </div>
                            <%if(locals.message && message.length>0){%>
                                <div class="alert alert-danger text-center">
                                    <%=message%>
                                </div>
                            <%}%>
                            <div class="mb-3">
                                <a href="/forgot-password" class="float-end font-sm forgot-password-link">Forgot password?</a>
                                <label class="form-check">
                                    <input type="checkbox" class="form-check-input" checked="">
                                    <span class="form-check-label">Remember</span>
                                </label>
                            </div>
                            <div class="mb-4">
                                <button type="submit" class="btn btn-primary w-100" style="max-width: 200px; margin: 0 auto; display: block;"> Login </button>
                            </div>
                        </form>
                        <p class="text-center small text-muted mb-15">or sign up with</p>
                        <div class="d-grid gap-3 mb-4">
                            <ul class="btn-login list_none text-center mb-15">
                                <li><a href="" class="btn btn-facebook hover-up mb-lg-0 mb-sm-4">Login With Facebook</a></li>
                                <div>
                                    <p>or</p>
                                </div>
                                <li><a href="/google" class="btn btn-google hover-up">Login With Google</a></li>
                            </ul>
                        </div>
                        <p class="text-center mb-4">Don't have account? <a href="/signup">Sign up</a></p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>